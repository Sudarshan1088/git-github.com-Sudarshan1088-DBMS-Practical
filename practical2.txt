-- Create Database 
CREATE DATABASE student; 
USE student; -- Create Customer Table 
CREATE TABLE Customer   
( 
);  
Customer_ID INT PRIMARY KEY,  
Name VARCHAR(50) NOT NULL,  
Email VARCHAR(100) UNIQUE,  
Phone VARCHAR(15) NOT NULL,   
Address VARCHAR(200) -- Create Restaurant Table 
CREATE TABLE Restaurant  
( 
Restaurant_ID INT PRIMARY KEY,  
Name VARCHAR(50) NOT NULL,  
Location VARCHAR(100),  
Contact VARCHAR(15) 
);  -- Create Orders Table 
CREATE TABLE Orders  
( 
);  
Order_ID INT PRIMARY KEY,   
Order_Date DATE NOT NULL,  
Status VARCHAR(20) CHECK (Status IN ('Pending','Delivered','Cancelled')),  
Customer_ID INT,  
Restaurant_ID INT,  
FOREIGN KEY (Customer_ID) REFERENCES Customer(Customer_ID),  
FOREIGN KEY (Restaurant_ID) REFERENCES Restaurant(Restaurant_ID) -- Insert Sample Data 
INSERT INTO Customer  
VALUES (1, 'Amit Sharma', 'amit@gmail.com', '9876543210', 'Pune');  
INSERT INTO Customer  
VALUES (2, 'Priya Mehta', 'priya@gmail.com', '9123456789', 'Mumbai');  
INSERT INTO Restaurant  
VALUES (101, 'Food Palace', 'Pune', '9988776655');  
INSERT INTO Restaurant  
VALUES (102, 'Tandoori Hub', 'Mumbai', '8877665544');  
INSERT INTO Orders  
VALUES (1001, '2025-09-25', 'Pending', 1, 101);  
INSERT INTO Orders  
VALUES (1002, '2025-09-26', 'Delivered', 2, 102);  
INSERT INTO Orders  
VALUES (1003, '2025-09-27', 'cancelled', 2, 102);  -- Create View for Active (Pending) Orders 
CREATE VIEW ActiveOrders AS  
SELECT Order_ID, Order_Date, Status  
FROM Orders  
WHERE Status = 'Pending'; 
CREATE INDEX idx_customer_email ON Customer(Email);  